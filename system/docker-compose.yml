services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: stockinvestmentdss-backend
    env_file:
      - .env
    ports:
      - "${BACKEND_PORT:-8001}:8000"
    volumes:
      - ./runtime-data:/app/runtime-data
    restart: unless-stopped